#include "main.h"
/**
  * add - Entry point.
  * @i: arguments that is added
  * @k: argument that is added
  * Description: a function that adds two integers.
  * Return: Always 0 (Success)
  */
int add(int i, int k)
{
return (i + k);
}
