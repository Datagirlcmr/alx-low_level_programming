#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int alph;

	for (alph = 48; alph < 58; alph++)
		putchar(alph);

	putchar('\n');

	return (0);
}
